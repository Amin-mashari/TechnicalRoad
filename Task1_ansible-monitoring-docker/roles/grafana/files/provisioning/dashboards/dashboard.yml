apiVersion: 1

providers:
  - name: 'Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10  # Periodically checks for dashboard updates
    options:
      path: /etc/grafana/provisioning/dashboards  # Path to dashboard files
